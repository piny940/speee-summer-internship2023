- content_for(:breadcrumbs) do
  = render 'application/breadcrumbs'
- content_for(:noindex, true)

%h1.text-center ベテランすまい

.prefectures.mt-5.centered-content
  - @prefectures.each do |prefecture|
    .prefecture.mb-2
      = render 'prefectures/cities', prefecture: prefecture
